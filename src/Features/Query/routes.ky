// The Query feature route table: run a statement (POST /query). The editor panel is
// now part of the shell (layout.kyx), so there is no GET panel route.
import Shared.AppContext;
import Features.Query.handler;

pub fn registerQuery(ctx: AppContext): void {
    ctx.app.post("/query", RunQueryHandler(ctx.conn));
}
