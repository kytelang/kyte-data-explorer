// The Connections feature route table: serve the form and handle the connect attempt.
import Shared.AppContext;
import Features.Connections.handler;

pub fn registerConnections(ctx: AppContext): void {
    ctx.app.get("/connect", GetConnectFormHandler());
    ctx.app.post("/connect", ConnectHandler(ctx.conn));
}
